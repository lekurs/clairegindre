security:
    encoders:
          App\Domain\Models\User:
                algorithm: bcrypt

    providers:
        our_db_provider:
                    entity:
                        class: App\Domain\Models\User
                        property: email

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false

        main:
            guard:
                authenticators:
                    - App\UI\Action\Security\UserConnectionAction

            anonymous: ~
            logout_on_user_change: true
            provider: our_db_provider
            form_login:
                login_path: adminLogin #Route vers le responder qui renvoie le login form
                check_path: adminLogin # Route qui verifie le username et le password
                default_target_path: index
            logout:
                path: logout
                target: index

    access_control:
#         - { path: ^/admin, roles: ROLE_ADMIN }
#         - { path: ^/, roles: ROLE_USER }

