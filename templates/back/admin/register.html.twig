{% extends('admin_layout.html.twig') %}

{%  block header %}
    {{ parent() }}
    {% block title %}
    <title>Ajouter un nouvel utilisateur</title>
    {%  endblock %}
{% endblock %}

{% block content %}
{{ form_start(register) }}
{{ form_widget(register) }}
{#{{ form_row(register.online) }}#}
    <button type="submit" name="register-submit" class="btn btn-primary">Enregistrer</button>
{{ form_end(register) }}

{% endblock %}