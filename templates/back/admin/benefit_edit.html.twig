{% extends "admin_layout.html.twig" %}

{%  block head %}
    {{ parent() }}
    {% block title %}
        <title>Administration des familles</title>
    {% endblock %}
{% endblock %}

{% block content %}
    <section>
        <div class="container">
            <div class="col">
                <h1>Administration des catégories de prestations</h1>
            </div>
            <div class="row">
                <div class="col">
                    <h2 class="text-uppercase">Prestations actuelles</h2>
                </div>
            </div>
            <div class="row">
                <div class="col">
                    <h2>Modifier une prestation</h2>
                </div>
            </div>
            <div class="row">
                <div class="col benefit-form">
                    {{ form_start(benefitType) }}
                    <input type="text" class="input-group" value="{{ benefits.name }}" />
                    <button type="submit" class="btn btn-primary submit-form">Modifier</button>
                {{ form_end(benefitType) }}
                </div>
            </div>
        </div>
    </section>
{% endblock %}